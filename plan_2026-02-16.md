# Barnes–Hut Simulation Implementation Plan

## Goal
Implement a simpler, faster Barnes–Hut simulation with a clear trade-off knob for speed vs accuracy (`θ`) and a small, maintainable code surface.

## Assumptions to keep and verify (no hidden defaults)
- Performance baseline: target Barnes–Hut behavior as \(O(n \log n)\) vs direct \(O(n^2)\) for large N, then measure crossover point locally.
- Approximation control: use only documented MAC criterion `s/d < θ` from Barnes–Hut references.
- Accuracy fallback: `θ = 0` is treated as direct-sum behavior and is required for verification, not for production use.
- Parameter defaults are not fixed assumptions:
  - `θ` values are initial settings only (benchmark will decide defaults per workload).
  - Softening `ε` starts as a configuration default, then tuned by test stability.
- Rust core as implementation source of truth:
  - use the 2D/3D module structure (`barnes_hut_2d`, `barnes_hut_3d`) as a reusable API pattern only.
  - the force-closure pattern expects non-normalized distance vectors from tree traversal.
- Runtime model: keep `Bun` as orchestrator only; numeric loops must stay in Rust.
- Optional rendering is offline and deterministic; no real-time renderer is planned in the core loop.

## Reference source for core algorithm/math
- Canonical paper: *A hierarchical O(N log N) force-calculation algorithm* (Barnes & Hut, 1986), DOI `10.1038/324446a0`.
- Core idea reference: https://en.wikipedia.org/wiki/Barnes%E2%80%93Hut_simulation
- Rust starter reference: https://docs.rs/nbody_barnes_hut/latest/nbody_barnes_hut/ (`barnes_hut_2d`, `OctTree`)

### Core math to follow (assumption-checked)
- Pair force with softening:
  - `r = p_j - p_i`
  - `r2 = dot(r, r) + ε²`
  - `f_ij = G * m_i * m_j * r / (r2 * sqrt(r2))`
- Center of mass for node:
  - `M = Σ m_k`
  - `CM = (Σ m_k * x_k) / M`
- Barnes–Hut acceptance:
  - Let `s = node_size`, `d = ||CM - p_i||`
  - Approximate node if `s/d < θ`
  - If `θ = 0`, algorithm degenerates to direct O(n²).

### Minimal code template (reference shape)
- 2D quad node fields:
  - bounds (`xmin`, `xmax`, `ymin`, `ymax`)
  - `mass`, `com_x`, `com_y`
  - `children[4]` indexes (`i32` sentinel `-1` for empty)
  - `body_idx` (`i32`, `-1` none)
- Particle storage (SoA for speed):
  - `x[] y[] vx[] vy[] m[]`
- Main loop per step:
  - `build_tree` → `compute_node_mass_com` → `accumulate_forces` → `integrate`

## Current status (2026-02-16)
- ✅ Direct-force baseline + Barnes–Hut path are implemented.
- ✅ Memory telemetry added for deterministic memory footprint reporting.
- ✅ Optional frame recording CLI is implemented (`--record`, `--frames-dir`, `--width`, `--height`, `--fps`, `--every-steps`).
- ✅ Preflight node-capacity validation and hard-stop diagnostics are implemented in the Barnes–Hut builder.
- ✅ Throughput-oriented run metrics now include `total_ms`, `avg_step_ms`, `steps_per_sec`, and `ns_per_particle_force`.
- ✅ `scripts/render_video.sh` added for high-FPS offline encoding.
- ✅ Memory cap guard now available via `--max-memory-mib` for predictable bounded-memory runs.
- ✅ Release build is the default runtime mode for `bun run nq` (`run.ts` passes `--release`, with `--debug` override).
- ✅ Benchmark sweep helper added (`scripts/bench_sweep.sh`) and `bun run bench`.
- ✅ Barnes–Hut force accumulation supports `--threads`.
- ✅ Deterministic benchmark wrapper script added for reproducible sweep CSV outputs (`scripts/bench_sweep_deterministic.sh`).
- ✅ Gravity scale (`--g`) integrated into force and energy evaluation paths.
- ✅ Initial-condition and mass profile knobs added (`--init*`, `--mass-profile*`) with seeded generation path in `ParticleSoa`.
- ✅ Integrator and diagnostics expanded (`--integrator`, optional `rk2`, momentum/energy telemetry).

## Milestone 0 — Scope and baseline contract (0.5 day)
- Decide dimensionality (start with 2D).
- Fix force model:
  - \(F = G m_i m_j / (r^2 + \epsilon^2)\)
  - Optional support for 3D can be deferred.
- Choose integration method (default: leapfrog/velocity Verlet).
- Define runtime parameters:
  - `θ` (opening angle), `softening ε`, `dt`, optional max depth / rebuild policy.
- Define acceptance checks:
  - correctness metric vs direct O(n²),
  - wall-clock target per step,
  - energy drift trend threshold over short horizon.

## Milestone 1 — Baseline and validation harness (0.5–1 day)
- Implement or retain an O(n²) direct-force path behind a flag.
- Add deterministic seed and benchmark seeds.
- Add diagnostics:
  - max/mean force error against direct method,
  - total energy drift,
  - step time split (`build`, `force`, `integrate`).
- Freeze one or two canonical scenarios for regression.

## Milestone 2 — Simple Barnes–Hut data model (1 day)
- Implement SoA particle storage:
  - `x[]`, `y[]`, `vx[]`, `vy[]`, `m[]`.
- Build fixed-size node pool reused every frame:
  - region bounds, child indices (4 for quad),
  - aggregate mass and COM,
  - leaf body index or empty flag.
- Add frame-local memory reuse for traversal stacks and temporary arrays.

## Milestone 3 — Tree construction and force traversal (1–2 days)
- Per step:
  1. compute bounds,
  2. build quadtree (iterative or recursive insert),
  3. compute node mass + COM bottom-up.
- Force loop:
  - traverse from each particle,
  - if node satisfies `s/d < θ`, use aggregated node force,
  - else recurse children.
- Include softening in denominator and minimum-distance guard.

## Milestone 4 — Performance pass (1 day)
- Remove avoidable allocations (preallocate once, clear/reuse).
- Replace deep recursion in hot loops with iterative stack where easy.
- Co-locate frequently accessed arrays for cache friendliness.
- Tune by measurement:
  - run short sweeps on candidate `θ` values (for example `0.3, 0.5, 0.7, 1.0`),
  - choose the smallest `θ` meeting speed/accuracy targets.
- Track recorder I/O separately (throughput, dropped-frame risk) so physics benchmarks remain comparable.

## Milestone 5 — Verification and tuning (0.5–1 day)
- Run convergence checks over fixed datasets:
  - direct vs Barnes–Hut force vector error,
  - short-horizon position/energy divergence.
- Sweep `(θ, ε, dt)` and record Pareto points for speed/accuracy.
- Document recommended presets (fast / balanced / accurate).
- Add hard-stop test for max-node-capacity boundary and deterministic failure message.

## Milestone 5a — HD capture and post-process guidance
- Add a clear render pipeline section in README with `ffmpeg` presets.
- Added `scripts/render_video.sh` helper to encode frames at common resolution/FPS profiles (including 30 and 60 fps).

## Milestone 6 — Optional next phase
- 3D octree variant.
- Thread-level parallel force evaluation via threaded Barnes–Hut `--threads`.
- Optional adaptive `θ` by local density.
- Optional higher-order integrator option.
- Momentum and sampled-energy conservation telemetry as first-class outputs.

## Next implementation plan (post-current state)

- [x] Add explicit CSV summary emitter schema for diagnostics-heavy logs (energy + momentum fields + sampled pair metadata).
- [x] Add explicit parser tests for `--init` and `--mass-profile` combinations at CLI level.
- [x] Add a regression validation case for RK2 path parity against direct for conservative scenarios.
- [x] Add docs/examples for full "video-first" workflow with ultra-large frame counts and fixed-step capture budgets.

## Deliverable checklist
- [x] Reproducible CLI or script to run deterministic benchmark set.
- [x] Barnes–Hut path is default for large N.
- [x] O(n²) baseline retained for debug/verification.
- [x] Metrics logged each run (`N`, step ms, error, energy drift).
- [x] Initial-condition and mass profile controls available at runtime.
- [x] Integrator selection (`leapfrog|verlet|rk2`) supported.
- [x] Energy and momentum diagnostics captured in summary output.

## Full physics/features backlog (next build phase)

### P0 — Runtime correctness and observability hardening (next)
- Add direct-vs-Barnes-Hut force-error and drift checks in a dedicated regression suite.
- Add RK2 parity regression for both particle-count scales used in CI smoke and release.
- Expand smoke scripts with explicit direct, Barnes-Hut, record, and long-run capture coverage.
- Keep CSV schema and README field tables as API contracts in CI.

### P1 — Physics expansion
- Add optional 3D execution path behind a dedicated `--dim` flag (default 2D).
- Add adaptive `--theta` policy controls (fixed/constant/local-density).
- Add initial-condition/velocity profile family extensions (e.g., rotating disk families).
- Add optional softening policy profiles for small-scale clustering control.

### P2 — Performance expansion
- Introduce deterministic thread chunking plus traversal cache tuning for `--threads > 1`.
- Add optional SIMD-friendly force kernel behind feature-gated build profile.
- Add explicit preset sweeps in docs for fast/balanced/accurate workloads.

### P3 — Long-run production UX
- Add convenience launch presets for `run.ts` (`--preset fast|balanced|accurate`).
- Add one-click render preset profiles and frame-rate budgeting helpers for 30/60 fps workflows.
